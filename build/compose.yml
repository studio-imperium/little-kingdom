services:
  backend:
    image: williamqm/mmobackend:latest
  caddy:
    image: caddy:latest
    ports: ["443:443", "80:80"]
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
volumes: { caddy_data: }